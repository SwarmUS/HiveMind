# Types used for the description of function
# Ex:
# var args_description = {
#    .0 = {.arg_int=types.INT}, // We just pass value so the type is registered
#    .1 = {.arg_float=types.FLOAT} // Same here
# };
# register_closure("someFunction", someClojure, args_description, nil);
types = {
  .INT = 0,
  .FLOAT = 0.0
};


# True and false global variable to make code more readable
true = 1;
false = nil;
