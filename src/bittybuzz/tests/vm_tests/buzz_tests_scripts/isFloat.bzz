function init() {
}


function step() {
    var nilVal = nil;
    var intVal = 42;
    var floatVal = 42.24;
    var stringVal = "Hello";
    var tableVal = {.0=42, .1=43};
    var nativeClosureVal = functionType;
    var lambdaClosureVal = function(){};

    assertFalse(isFloat(nilVal));
    assertFalse(isFloat(intVal));
    assertTrue(isFloat(floatVal));
    assertFalse(isFloat(stringVal));
    assertFalse(isFloat(tableVal));
    assertFalse(isFloat(nativeClosureVal));
    assertFalse(isFloat(lambdaClosureVal));
}
