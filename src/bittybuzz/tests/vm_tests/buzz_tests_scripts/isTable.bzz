function init() {
}


function step() {
    var nilVal = nil;
    var intVal = 42;
    var floatVal = 42.24;
    var stringVal = "Hello";
    var tableVal = {0=42, 1=43};
    var nativeClosureVal = functionType;
    var lambdaClosureVal = function(){};

    assertFalse(isTable(nilVal));
    assertFalse(isTable(intVal));
    assertFalse(isTable(floatVal));
    assertFalse(isTable(stringVal));
    assertTrue(isTable(tableVal));
    assertFalse(isTable(nativeClosureVal));
    assertFalse(isTable(lambdaClosureVal));
}
