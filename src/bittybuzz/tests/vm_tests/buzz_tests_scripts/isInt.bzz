function init() {
}


function step() {
    var nilVal = nil;
    var intVal = 42;
    var floatVal = 42.24;
    var stringVal = "Hello";
    var tableVal = {.0=42, .1=43};
    var nativeClosureVal = functionType;
    var lambdaClosureVal = function(){};

    assertFalse(isInt(nilVal));
    assertTrue(isInt(intVal));
    assertFalse(isInt(floatVal));
    assertFalse(isInt(stringVal));
    assertFalse(isInt(tableVal));
    assertFalse(isInt(nativeClosureVal));
    assertFalse(isInt(lambdaClosureVal));
}
