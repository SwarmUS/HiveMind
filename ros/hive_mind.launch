<?xml version="1.0"?>
    <!-- Generic launch file for a hivemind node -->
    <!-- Template to be used for further more complexed setups -->
    <launch>
        <!-- Simple node -->
        <node name="hive_mind" pkg="hive_mind" type="hive_mind"  output="screen" >
            <param name="board_uuid" value="1" />

            <param name="host_tcp_address" value="127.0.0.1" />
            <param name="host_tcp_port" value="5555" />

            <!-- Set port to 0 to disable server -->
            <param name="remote_mock_port" value="9001" />

            <param name="log_level" value="Info" />

            <!-- Interloc refresh rate in Hz -->
            <param name="interloc_refresh_rate" value="10.0" />
        </node>
</launch>
